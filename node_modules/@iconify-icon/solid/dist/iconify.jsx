/**
 * Export functions
 */
export { enableCache, disableCache, iconExists, getIcon, listIcons, addIcon, addCollection, calculateSize, buildIcon, loadIcons, loadIcon, addAPIProvider, appendCustomStyle, _api, } from 'iconify-icon';
/**
 * Solid component
 */
export function Icon(props) {
    let { icon, mode, inline, rotate, flip, width, height, preserveAspectRatio, } = props;
    // Convert icon to string
    if (typeof icon === 'object') {
        icon = JSON.stringify(icon);
    }
    return (
    // @ts-ignore
    <iconify-icon attr:icon={icon} attr:mode={mode} attr:inline={inline} attr:rotate={rotate} attr:flip={flip} attr:width={width} attr:height={height} attr:preserveAspectRatio={preserveAspectRatio} {...props}/>);
}
